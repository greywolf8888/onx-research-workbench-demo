# Demo Data Policy

The `demo_data/` directory contains synthetic fixtures for public review.
The schema resembles the private workbench contract, but values are reduced,
anonymized, and safe to publish.

## 中文摘要

`demo_data/` 仅包含公开演示用合成样本。结构参考工作台合约，但数值已降级和脱敏。
